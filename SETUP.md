# 开发环境配置指南

本文档帮助新的开发者快速配置开发环境并运行项目。

## 📋 前置要求

### 必需软件

1. **Node.js 14+**
   - 下载地址: https://nodejs.org/
   - 推荐使用 LTS 版本
   - 验证安装: `node -v` 和 `npm -v`

2. **HBuilderX**（推荐）
   - 下载地址: https://www.dcloud.io/hbuilderx.html
   - 或使用 Vue CLI: `npm install -g @vue/cli`

3. **微信开发者工具**（如需开发微信小程序）
   - 下载地址: https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

### 可选软件

- **Git**: 版本控制
- **VS Code**: 代码编辑器（如不使用 HBuilderX）

---

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/your-repo/smart-recipe-uniapp.git
cd smart-recipe-uniapp
```

### 2. 安装依赖

```bash
npm install
```

**常见问题**:
- 如果安装速度慢，可以使用淘宝镜像:
  ```bash
  npm config set registry https://registry.npmmirror.com
  npm install
  ```
- 如果遇到权限问题（Windows），以管理员身份运行命令行

### 3. 配置后端 API 地址

编辑 `api/request.js` 文件，修改 `BASE_URL`:

```javascript
// 开发环境 - 本地后端
const BASE_URL = 'http://127.0.0.1:8000/api'

// 或使用团队共享的开发服务器
// const BASE_URL = 'http://dev-server-ip:8000/api'
```

### 4. 启动开发服务器

#### 方式一：使用 HBuilderX（推荐）

1. 打开 HBuilderX
2. 文件 → 导入 → 从本地目录导入
3. 选择 `smart-recipe-uniapp` 目录
4. 运行 → 运行到浏览器 / 运行到小程序模拟器

#### 方式二：使用命令行

```bash
# 运行到 H5
npm run dev:h5

# 运行到微信小程序
npm run dev:mp-weixin
```

### 5. 微信小程序配置（如需）

1. 打开微信开发者工具
2. 导入项目
3. 项目路径选择: `smart-recipe-uniapp/dist/dev/mp-weixin`
4. AppID: 使用测试号或配置自己的 AppID（在 `manifest.json` 中）

---

## 🔧 常见问题

### 问题 1: `npm install` 失败

**原因**: 网络问题或权限问题

**解决方法**:
```bash
# 清除缓存
npm cache clean --force

# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### 问题 2: 运行时提示 "Cannot find module 'pinia'"

**原因**: 依赖未正确安装

**解决方法**:
```bash
# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题 3: API 请求失败（Network Error）

**原因**: 后端服务未启动或 API 地址配置错误

**解决方法**:
1. 确保后端服务已启动（参考后端 README.md）
2. 检查 `api/request.js` 中的 `BASE_URL` 配置
3. 检查网络连接

### 问题 4: 微信小程序真机预览白屏

**原因**: 域名未配置或未开启调试模式

**解决方法**:
1. 微信开发者工具 → 详情 → 本地设置 → 勾选"不校验合法域名"
2. 或在微信公众平台配置服务器域名

### 问题 5: HBuilderX 无法识别项目

**原因**: 项目结构不符合 HBuilderX 要求

**解决方法**:
1. 确保项目根目录有 `manifest.json` 和 `pages.json`
2. 重新导入项目
3. 或使用命令行方式运行

---

## 📁 项目结构说明

```
smart-recipe-uniapp/
├── api/                  # API 接口封装
├── components/           # 公共组件
├── pages/                # 页面文件
├── store/                # Pinia 状态管理
├── utils/                # 工具函数
├── static/               # 静态资源
├── App.vue               # 应用入口
├── main.js               # 主文件
├── manifest.json         # 应用配置
├── pages.json            # 页面路由配置
└── package.json          # 项目依赖
```

---

## 🔗 相关文档

- [UniApp 官方文档](https://uniapp.dcloud.net.cn/)
- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Pinia 官方文档](https://pinia.vuejs.org/zh/)
- [项目 README](./README.md)
- [后端项目文档](../smart_recipe_server/README.md)

---

## 🆘 获取帮助

如果遇到其他问题：

1. 查看 [README.md](./README.md)
2. 查看 [后端 SETUP.md](../smart_recipe_server/SETUP.md)
3. 搜索 [UniApp 官方论坛](https://ask.dcloud.net.cn/)
4. 联系项目负责人

---

## ✅ 验证安装

运行以下命令验证环境配置是否正确：

```bash
# 检查 Node.js 版本
node -v
# 应输出: v14.x.x 或更高

# 检查 npm 版本
npm -v
# 应输出: 6.x.x 或更高

# 检查依赖是否安装
npm list --depth=0
# 应显示所有依赖包
```

---

**最后更新**: 2026-02-26
