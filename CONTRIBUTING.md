# 贡献指南

感谢您对智能食谱项目的贡献！本文档将帮助您了解项目的开发规范和流程。

## 📝 开发规范

### 代码规范

#### 命名规范

- **组件文件**: 大驼峰命名 `RecipeCard.vue`
- **页面文件**: 小写短横线 `my-recipes.vue`
- **JS 文件**: 小写短横线 `request.js`
- **变量/函数**: 小驼峰 `getUserInfo`
- **常量**: 大写下划线 `BASE_URL`
- **CSS 类名**: 小写短横线 `recipe-card`

#### 文件组织

```javascript
// Vue 组件结构顺序
<template>
  <!-- 模板 -->
</template>

<script>
/**
 * 组件说明
 */
export default {
  name: 'ComponentName',
  components: {},
  props: {},
  data() {},
  computed: {},
  watch: {},
  created() {},
  mounted() {},
  methods: {}
}
</script>

<style lang="scss" scoped>
/* 样式 */
</style>
```

#### 注释规范

```javascript
/**
 * 函数说明
 * @param {String} username - 用户名
 * @param {Number} age - 年龄
 * @returns {Object} 用户信息
 */
function getUserInfo(username, age) {
  // 实现代码
}
```

### Git 提交规范

#### 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型

- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

#### 示例

```bash
# 新功能
git commit -m "feat(user): 添加用户登录功能"

# 修复 Bug
git commit -m "fix(recipe): 修复食谱详情页图片加载失败问题"

# 文档更新
git commit -m "docs: 更新 README 安装说明"

# 代码格式
git commit -m "style: 统一代码缩进为 2 空格"
```

---

## 🔄 开发流程

### 1. 创建分支

```bash
# 从 main 分支创建新分支
git checkout main
git pull origin main
git checkout -b feature/your-feature-name
```

### 2. 开发功能

- 遵循代码规范
- 添加必要的注释
- 确保代码可以正常运行

### 3. 提交代码

```bash
# 查看修改
git status

# 添加文件
git add .

# 提交（遵循提交规范）
git commit -m "feat(module): 功能描述"
```

### 4. 推送到远程

```bash
git push origin feature/your-feature-name
```

### 5. 创建 Pull Request

1. 在 GitHub/GitLab 上创建 Pull Request
2. 填写 PR 描述（做了什么、为什么、如何测试）
3. 等待代码审查
4. 根据反馈修改代码
5. 合并到主分支

---

## 🧪 测试规范

### 功能测试

在提交代码前，请确保：

- [ ] 功能正常运行
- [ ] 没有控制台错误
- [ ] API 请求正常
- [ ] 页面样式正确
- [ ] 兼容微信小程序和 H5

### 测试步骤

```bash
# 1. 启动后端服务
cd smart_recipe_server
uv run python manage.py runserver

# 2. 启动前端服务
cd smart-recipe-uniapp
npm run dev:h5

# 3. 测试功能
# 在浏览器中测试所有功能点
```

---

## 📦 依赖管理

### 添加新依赖

```bash
# 安装依赖
npm install package-name

# 提交 package.json 和 package-lock.json
git add package.json package-lock.json
git commit -m "chore: 添加 package-name 依赖"
```

### 更新依赖

```bash
# 查看过期的依赖
npm outdated

# 更新依赖
npm update

# 或更新到最新版本
npm install package-name@latest
```

---

## 🐛 Bug 修复流程

### 1. 重现 Bug

- 确认 Bug 存在
- 记录重现步骤
- 截图或录屏

### 2. 定位问题

- 查看控制台错误
- 检查网络请求
- 使用调试工具

### 3. 修复 Bug

- 创建 bugfix 分支
- 修复问题
- 测试验证

### 4. 提交修复

```bash
git checkout -b bugfix/issue-description
# 修复代码
git commit -m "fix(module): 修复具体问题描述"
git push origin bugfix/issue-description
```

---

## 📚 常用命令

### 开发命令

```bash
# 安装依赖
npm install

# 启动 H5 开发服务器
npm run dev:h5

# 启动微信小程序开发
npm run dev:mp-weixin

# 构建生产版本
npm run build:h5
npm run build:mp-weixin
```

### Git 命令

```bash
# 查看状态
git status

# 查看分支
git branch

# 切换分支
git checkout branch-name

# 拉取最新代码
git pull origin main

# 合并分支
git merge branch-name

# 查看提交历史
git log --oneline
```

---

## 🔍 代码审查清单

在提交 PR 前，请自查：

### 功能
- [ ] 功能完整实现
- [ ] 没有遗留的 console.log
- [ ] 没有注释掉的代码
- [ ] 错误处理完善

### 代码质量
- [ ] 遵循命名规范
- [ ] 添加必要注释
- [ ] 代码可读性好
- [ ] 没有重复代码

### 性能
- [ ] 没有不必要的请求
- [ ] 图片已压缩
- [ ] 列表使用分页

### 兼容性
- [ ] H5 测试通过
- [ ] 微信小程序测试通过
- [ ] 不同屏幕尺寸正常显示

---

## 💡 最佳实践

### 1. 组件化开发

- 将可复用的 UI 抽取为组件
- 组件职责单一
- 使用 props 传递数据
- 使用 emit 触发事件

### 2. 状态管理

- 全局状态使用 Pinia
- 页面状态使用 data
- 避免过度使用全局状态

### 3. API 调用

- 使用封装好的 API 方法
- 添加 loading 状态
- 处理错误情况
- 避免重复请求

### 4. 样式编写

- 使用 SCSS 变量
- 避免内联样式
- 使用 scoped 避免样式污染
- 响应式设计（rpx 单位）

---

## 🆘 获取帮助

遇到问题时：

1. 查看项目文档（README.md、SETUP.md）
2. 搜索已有的 Issue
3. 询问团队成员
4. 创建新的 Issue

---

## 📞 联系方式

- 项目负责人: [联系方式]
- 技术讨论群: [群号/链接]
- Issue 提交: [GitHub/GitLab 链接]

---

**感谢您的贡献！** 🎉
